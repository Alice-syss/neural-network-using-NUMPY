A neural network from scratch using only NumPy
Forward pass — data flowing through layers
Backpropagation — error flowing backwards
Gradient descent — weights updating every loop
A loss curve proving it actually learned
